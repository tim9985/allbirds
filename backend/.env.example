# 서버 설정
PORT=4000
NODE_ENV=development

# MongoDB 설정
MONGODB_URI=mongodb://127.0.0.1:27017/allbirdsDB

# 세션 설정
SESSION_SECRET=your-secret-key-here
SESSION_TTL_MINUTES=30

# CORS 허용 Origin (쉼표로 구분)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# bcrypt 설정
BCRYPT_ROUNDS=10
